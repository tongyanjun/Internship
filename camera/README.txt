在python3.6环境下运行

首先安装所需包，打开cmd窗口进入camera文件夹输入命令：

pip install -r requirements.txt

安装后运行命令：

python main.py

程序会打开一个窗口，播放电脑摄像头所显示的内容

按‘c’键进行拍摄，拍摄图片自动保存在camera文件夹内，命名方式为‘年月日-时分秒’， 如‘20191204-114628’

长按‘q键’关闭窗口程序结束运行